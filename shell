#!/usr/bin/env bash
# Drop into (or run a command inside) the Nix dev shell defined in .nix/flake.nix.
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd -P)"

if [[ $# -gt 0 ]]; then
  exec nix develop "$repo_root/.nix" --command "$@"
fi

exec nix develop "$repo_root/.nix"
